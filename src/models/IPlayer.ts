interface IPlayer {
    name: string;
    teamName: string;
    position: string;
    starting: boolean;
    injuredReserve: boolean;
    // name, team name, position, starting?
    // Injured Reserve
    
}

export default IPlayer;